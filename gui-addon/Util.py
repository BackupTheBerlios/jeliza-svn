#!/usr/bin/env python

# Python imports
import os
import sys
import shutil

class Util:
    def newProfile(self, p):
        """ Im Monemt nur ein Profil moeglich """

    def getProfilePath(self, p):
        return "profiles/" + p

    def getCurrentProfile(self):
#        return os.environ["jeliza.profile"]
        return ""

    def chProfile(self, p):
        """ Im Monemt nur ein Profil moeglich """
    