if ( sub == "du" && verb == "bist" && obj == "" && qwrd == "was" ) ; then
 sub = du
 verb = bist
 obj = doof
 qwrd = 
end
if ( sub == "nein" && verb == "ist" && obj == "" && qwrd == "was" ) ; then
 sub = nein
 verb = ist
 obj = das Ich nicht
 qwrd = 
end
if ( sub == "nein das europa" && verb == "ist" && obj == "" && qwrd == "was" ) ; then
 sub = Nein das Europa
 verb = ist
 obj = nicht so
 qwrd = 
end
if ( sub == "nein das" && verb == "ist" && obj == "" && qwrd == "was" ) ; then
 sub = Nein das
 verb = ist
 obj = kein Bloedsinn
 qwrd = 
end
if ( sub == "der" && verb == "war" && obj == "" && qwrd == "was" ) ; then
 sub = der
 verb = war
 obj = nix versuchs nochma
 qwrd = 
end
if ( sub == "und jez lass mish in ruhe du nervst wer auch imma du" && verb == "bist" && obj == "" && qwrd == "was" ) ; then
 sub = und jez lass mish in ruhe du nervst wer auch imma du
 verb = bist
 obj = null
 qwrd = 
end
if ( sub == "ja sicher doch" && verb == "bist" && obj == "" && qwrd == "was" ) ; then
 sub = ja sicher doch
 verb = bist
 obj = du dumm
 qwrd = 
end
if ( sub == "ich glaube, du" && verb == "bist" && obj == "" && qwrd == "was" ) ; then
 sub = Ich glaube, du
 verb = bist
 obj = nicht intelligent
 qwrd = 
end
if ( sub == "man merkt dir an, dass du kein mensch" && verb == "bist" && obj == "" && qwrd == "was" ) ; then
 sub = Man merkt dir an, dass du kein Mensch
 verb = bist
 obj = null
 qwrd = 
end
if ( sub == "ich weiss nicht, aber du" && verb == "bist" && obj == "" && qwrd == "was" ) ; then
 sub = Ich weiss nicht, aber du
 verb = bist
 obj = doch geschaffen worden, um wie ein Mensch zu reden
 qwrd = 
end
if ( sub == "ich glaube, das wie" && verb == "ist" && obj == "" && qwrd == "was" ) ; then
 sub = Ich glaube, das Wie
 verb = ist
 obj = sinnlos, mit dir zu reden
 qwrd = 
end
if ( sub == "dies" && verb == "ist" && obj == "" && qwrd == "was" ) ; then
 sub = dies
 verb = ist
 obj = ein sinnvolles gespr�ch
 qwrd = 
end
if ( sub == "du" && verb == "bist" && obj == "" && qwrd == "wie" ) ; then
 sub = du
 verb = bist
 obj = doof
 qwrd = 
end
if ( sub == "nein" && verb == "ist" && obj == "" && qwrd == "wie" ) ; then
 sub = nein
 verb = ist
 obj = das Ich nicht
 qwrd = 
end
if ( sub == "nein das europa" && verb == "ist" && obj == "" && qwrd == "wie" ) ; then
 sub = Nein das Europa
 verb = ist
 obj = nicht so
 qwrd = 
end
if ( sub == "nein das" && verb == "ist" && obj == "" && qwrd == "wie" ) ; then
 sub = Nein das
 verb = ist
 obj = kein Bloedsinn
 qwrd = 
end
if ( sub == "der" && verb == "war" && obj == "" && qwrd == "wie" ) ; then
 sub = der
 verb = war
 obj = nix versuchs nochma
 qwrd = 
end
if ( sub == "und jez lass mish in ruhe du nervst wer auch imma du" && verb == "bist" && obj == "" && qwrd == "wie" ) ; then
 sub = und jez lass mish in ruhe du nervst wer auch imma du
 verb = bist
 obj = null
 qwrd = 
end
if ( sub == "ja sicher doch" && verb == "bist" && obj == "" && qwrd == "wie" ) ; then
 sub = ja sicher doch
 verb = bist
 obj = du dumm
 qwrd = 
end
if ( sub == "ich glaube, du" && verb == "bist" && obj == "" && qwrd == "wie" ) ; then
 sub = Ich glaube, du
 verb = bist
 obj = nicht intelligent
 qwrd = 
end
if ( sub == "man merkt dir an, dass du kein mensch" && verb == "bist" && obj == "" && qwrd == "wie" ) ; then
 sub = Man merkt dir an, dass du kein Mensch
 verb = bist
 obj = null
 qwrd = 
end
if ( sub == "ich weiss nicht, aber du" && verb == "bist" && obj == "" && qwrd == "wie" ) ; then
 sub = Ich weiss nicht, aber du
 verb = bist
 obj = doch geschaffen worden, um wie ein Mensch zu reden
 qwrd = 
end
if ( sub == "ich glaube, das wie" && verb == "ist" && obj == "" && qwrd == "wie" ) ; then
 sub = Ich glaube, das Wie
 verb = ist
 obj = sinnlos, mit dir zu reden
 qwrd = 
end
if ( sub == "dies" && verb == "ist" && obj == "" && qwrd == "wie" ) ; then
 sub = dies
 verb = ist
 obj = ein sinnvolles gespr�ch
 qwrd = 
end
if ( sub == "du" && verb == "bist" && obj == "" && qwrd == "wer" ) ; then
 sub = du
 verb = bist
 obj = doof
 qwrd = 
end
if ( sub == "nein" && verb == "ist" && obj == "" && qwrd == "wer" ) ; then
 sub = nein
 verb = ist
 obj = das Ich nicht
 qwrd = 
end
if ( sub == "nein das europa" && verb == "ist" && obj == "" && qwrd == "wer" ) ; then
 sub = Nein das Europa
 verb = ist
 obj = nicht so
 qwrd = 
end
if ( sub == "nein das" && verb == "ist" && obj == "" && qwrd == "wer" ) ; then
 sub = Nein das
 verb = ist
 obj = kein Bloedsinn
 qwrd = 
end
if ( sub == "der" && verb == "war" && obj == "" && qwrd == "wer" ) ; then
 sub = der
 verb = war
 obj = nix versuchs nochma
 qwrd = 
end
if ( sub == "und jez lass mish in ruhe du nervst wer auch imma du" && verb == "bist" && obj == "" && qwrd == "wer" ) ; then
 sub = und jez lass mish in ruhe du nervst wer auch imma du
 verb = bist
 obj = null
 qwrd = 
end
if ( sub == "ja sicher doch" && verb == "bist" && obj == "" && qwrd == "wer" ) ; then
 sub = ja sicher doch
 verb = bist
 obj = du dumm
 qwrd = 
end
if ( sub == "ich glaube, du" && verb == "bist" && obj == "" && qwrd == "wer" ) ; then
 sub = Ich glaube, du
 verb = bist
 obj = nicht intelligent
 qwrd = 
end
if ( sub == "man merkt dir an, dass du kein mensch" && verb == "bist" && obj == "" && qwrd == "wer" ) ; then
 sub = Man merkt dir an, dass du kein Mensch
 verb = bist
 obj = null
 qwrd = 
end
if ( sub == "ich weiss nicht, aber du" && verb == "bist" && obj == "" && qwrd == "wer" ) ; then
 sub = Ich weiss nicht, aber du
 verb = bist
 obj = doch geschaffen worden, um wie ein Mensch zu reden
 qwrd = 
end
if ( sub == "ich glaube, das wie" && verb == "ist" && obj == "" && qwrd == "wer" ) ; then
 sub = Ich glaube, das Wie
 verb = ist
 obj = sinnlos, mit dir zu reden
 qwrd = 
end
if ( sub == "dies" && verb == "ist" && obj == "" && qwrd == "wer" ) ; then
 sub = dies
 verb = ist
 obj = ein sinnvolles gespr�ch
 qwrd = 
end
if ( sub == "du" && verb == "bist" && obj == "" && qwrd == "wo" ) ; then
 sub = du
 verb = bist
 obj = doof
 qwrd = 
end
if ( sub == "nein" && verb == "ist" && obj == "" && qwrd == "wo" ) ; then
 sub = nein
 verb = ist
 obj = das Ich nicht
 qwrd = 
end
if ( sub == "nein das europa" && verb == "ist" && obj == "" && qwrd == "wo" ) ; then
 sub = Nein das Europa
 verb = ist
 obj = nicht so
 qwrd = 
end
if ( sub == "nein das" && verb == "ist" && obj == "" && qwrd == "wo" ) ; then
 sub = Nein das
 verb = ist
 obj = kein Bloedsinn
 qwrd = 
end
if ( sub == "der" && verb == "war" && obj == "" && qwrd == "wo" ) ; then
 sub = der
 verb = war
 obj = nix versuchs nochma
 qwrd = 
end
if ( sub == "und jez lass mish in ruhe du nervst wer auch imma du" && verb == "bist" && obj == "" && qwrd == "wo" ) ; then
 sub = und jez lass mish in ruhe du nervst wer auch imma du
 verb = bist
 obj = null
 qwrd = 
end
if ( sub == "ja sicher doch" && verb == "bist" && obj == "" && qwrd == "wo" ) ; then
 sub = ja sicher doch
 verb = bist
 obj = du dumm
 qwrd = 
end
if ( sub == "ich glaube, du" && verb == "bist" && obj == "" && qwrd == "wo" ) ; then
 sub = Ich glaube, du
 verb = bist
 obj = nicht intelligent
 qwrd = 
end
if ( sub == "man merkt dir an, dass du kein mensch" && verb == "bist" && obj == "" && qwrd == "wo" ) ; then
 sub = Man merkt dir an, dass du kein Mensch
 verb = bist
 obj = null
 qwrd = 
end
if ( sub == "ich weiss nicht, aber du" && verb == "bist" && obj == "" && qwrd == "wo" ) ; then
 sub = Ich weiss nicht, aber du
 verb = bist
 obj = doch geschaffen worden, um wie ein Mensch zu reden
 qwrd = 
end
if ( sub == "ich glaube, das wie" && verb == "ist" && obj == "" && qwrd == "wo" ) ; then
 sub = Ich glaube, das Wie
 verb = ist
 obj = sinnlos, mit dir zu reden
 qwrd = 
end
if ( sub == "dies" && verb == "ist" && obj == "" && qwrd == "wo" ) ; then
 sub = dies
 verb = ist
 obj = ein sinnvolles gespr�ch
 qwrd = 
end
